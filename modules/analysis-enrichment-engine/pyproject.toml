[project]
name = "sentinel-analysis-enrichment-engine"
version = "0.1.0"
requires-python = ">=3.12,<3.14"
dependencies = ["sentinel-contracts>=1.0,<2.0"]

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
