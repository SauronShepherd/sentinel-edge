[project]
name = "sentinel-incident-event-engine"
version = "0.1.0"
requires-python = ">=3.12,<3.14"
dependencies = ["sentinel-contracts>=0.1,<0.2"]

[tool.sentinel-edge]
incident_authority = true

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
