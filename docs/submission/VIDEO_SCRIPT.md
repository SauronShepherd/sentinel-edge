# Sentinel Edge video script

1. Show `arm64-doctor` from the frozen candidate and the persistent safety/emulation notices.
2. Run the zero-hardware demo and show all four hazard adapters.
3. Run the simultaneous-event scenario, including storage pressure, clock fault, source outage, and recovery.
4. Show explicit incident truth, evidence-unavailable/degraded state, and the signed scenario receipt.
5. Show the final `arm64-benchmark` B0/B1/O1 artifact (or `benchmark-replay` when demonstrating the bundled evidence), with `simulated` classification, absolute values, quality guardrails, and the explicit no-Raspberry-Pi-performance limitation.
6. Show optional collaborative detection disabled by default, then a simulated opt-in signal and its unverified transport boundary.
7. Close with the manual submission checklist and the statement that physical hardware was not used.
